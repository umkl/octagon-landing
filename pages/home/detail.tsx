import React from 'react'

interface Props {
    teaser: string;
    header: string;
    text: string;
    image: any;
}

function Detail(props: Props) {
    const {teaser, header, text, image} = props

    return (
        <section>
            
        </section>
    )
}

export default Detail
