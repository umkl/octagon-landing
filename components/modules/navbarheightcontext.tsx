import React from "react";
import {useState} from "react";

// const NavbarHeightContext = React.createContext();
export function NavbarHeightProvider({children}){
    const [height, setHeight] = useState<number>(0);
    return (<></>);
    // retrun (<>)
}